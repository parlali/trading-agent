# Convex -- the only local env vars needed
# All other secrets (API keys, broker credentials, etc.) are stored
# as Convex environment variables and accessed at runtime via Convex
CONVEX_DEPLOYMENT=
CONVEX_URL=
